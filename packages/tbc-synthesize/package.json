{
  "name": "@tbc-frameworx/tbc-synthesize",
  "version": "0.4.2",
  "description": "TBC Synthesize operations library",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build:src && bun build:types",
    "build:src": "bun build src/index.ts --outfile=dist/index.js --target=bun",
    "build:types": "bunx tsc --emitDeclarationOnly --outDir dist"
  },
  "devDependencies": {
    "@types/node": "^20.19.37",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hami-frameworx/core": "0.1.10",
    "pocketflow": "^1.0.4",
    "@tbc-frameworx/tbc-record": "^0.4.2"
  }
}