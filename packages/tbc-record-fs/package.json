{
  "name": "@tbc-frameworx/tbc-record-fs",
  "version": "0.1.0",
  "description": "TBC File System operations library",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build:src && bun build:types",
    "build:src": "bun build src/index.ts --outfile=dist/index.js --target=bun",
    "build:types": "bunx tsc --emitDeclarationOnly --outDir dist"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "pocketflow": "^1.0.4",
    "@hami-frameworx/core": "0.1.4",
    "@tbc-frameworx/tbc-record": "^0.1.0"
  }
}